%
% Basic core definitions used for DocBook
%
\usepackage[dvips]{graphicx}
\usepackage{ifthen}
\usepackage{subfigure}

%
% These <bookinfo|articleinfo> parameters must be defined
%
\newcommand{\DBKreference}{A Reference}     % mapped to <pubsnumber>
\newcommand{\DBKsite}{Anywhere}             % mapped to <bookinfo/address>
\newcommand{\DBKdate}{\today}               % mapped to <bookinfo/date>
\newcommand{\DBKreleaseinfo}{Working Paper} % mapped to <releaseinfo>
\newcommand{\DBKedition}{1}                 % mapped to <edition>
\newcommand{\DBKsubtitle}{}                 % mapped to <subtitle>

% Main latex <othercredit> command. It will be redefined by the XSL stylesheets.
\newcommand{\DBKindexation}{}

%
% Main latex <revhistory> command. It will be redefined by the XSL stylesheets.
%
\newcommand{\DBKrevhistory}{}

%
% If hyperlink, load the hyperref package and set the \hyperlabel command needed
% by the DocBook stylesheets
%
\@ifundefined{DBKhyperlink}{ \def\DBKhyperlink{no} }{}

\ifthenelse{\equal{\DBKhyperlink}{yes}}{
  \@ifundefined{hyperparam}{
    \def\hyperparam{linktocpage,colorlinks,citecolor=blue}
  }{}
  \@ifundefined{pdfoutput}{
    \usepackage[dvips,\hyperparam]{hyperref}
    \def\hyperlabel#1{\Hy@raisedlink{\hyper@anchorstart{#1}\hyper@anchorend}}
  }{
    \usepackage[pdftex,\hyperparam]{hyperref}
    \def\hyperlabel#1{\hypertarget{#1}{}}
  }
}{
  \usepackage{hyperref}
  \def\hyperlabel#1{}
}

\usepackage{dbk_biblio}
\usepackage{dbk_table}
\usepackage{dbk_changebar}
